# Machine Learning and Vision Libraries
torch==2.0.1
torchvision==0.15.2
opencv-python==4.9.0.80
pandas>=1.5.3
matplotlib>=3.6.0
pygame>=2.1.0
numpy<2.0.0
pyserial>=3.5

# GPIO and Camera Control (specific to Jetson)
gpiozero>=1.6.2
# Exclude picamera2 if you use OpenCV
# picamera2>=0.3.7

# RealSense SDK Python bindings
pyrealsense2>=2.50.0  # Make sure you have the right version for your setup

# ROS 2 Foxy specific packages
rclpy>=1.0.0  # ROS 2 Python client library
cv_bridge>=1.15.0  # For ROS to OpenCV conversions (depends on system package)
colcon-common-extensions>=0.3.0  # Build tool for ROS 2 packages
sensor_msgs>=1.13.0  # For handling sensor message types like Image

# System dependencies (for cv_bridge and other ROS packages)
# Install the following system packages separately:
# sudo apt install ros-foxy-cv-bridge ros-foxy-sensor-msgs python3-colcon-common-extensions
